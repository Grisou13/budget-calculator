
interface BudgetLineProps {
    withSlider: boolean;
}

const defaultProps: BudgetLineProps = {
    withSlider: false
}

function BudgetLine(props: BudgetLineProps = defaultProps){
    
}

export default BudgetLine;